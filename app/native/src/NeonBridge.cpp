#include "NeonBridge.h"

namespace neon::bridge {
void render() {
//	neon::catalystRender();
}

void justAnotherMethod() { std::cout << "Hello from C++" << std::endl; }

void onApplicationReady() { std::cout << "Ready!!!" << std::endl; }
} // namespace neon::bridge
